version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  userapi2:
    lang: node14-express-service
    handler: ./userapi2
    image: registry.tyap.cloud/userapi2:latest
    environment:
      read_timeout: 60s
      write_timeout: 60s

